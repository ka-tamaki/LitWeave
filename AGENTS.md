# AGENTS.md

## プロジェクト概要

LitWeaveは、論文PDF、書誌情報、読書状態、キーワード、Markdownメモおよび引用関係をローカルで管理するWindows向けアプリケーションです。

アプリ名は「LitWeave」、コンセプトは「論文を編み、知識をつなぐ」です。

## 仕様の正本

実装前に必ず以下を確認してください。

- `docs/requirements.md`
- `docs/technical-requirements.md`（存在する場合）
- `README.md`
- `CHANGELOG.md`

機能要件の正本は`docs/requirements.md`です。

コードと要件が異なる場合は、独断で要件を変更せず、差異を報告してください。仕様変更を実装した場合は、関連ドキュメントも同じ変更内で更新してください。

## 対象環境

- Windows 11
- PowerShell
- 1人で利用するローカルアプリ
- UIは日本語
- Box Driveをファイル保存先として使用

Windows以外の環境対応、クラウド公開、複数ユーザー対応を前提にしないでください。

## 初版の対象外

明示的な依頼がない限り、以下を実装しないでください。

- AI要約、AI検索
- PDF本文解析、全文検索
- PDF注釈
- アプリ内PDFビューア
- Zotero連携
- Box API連携
- DOIからの書誌情報自動取得
- 論文の自動ダウンロード
- クラウド共同編集
- 複数ユーザー
- モバイル版、macOS版

## データ保存の原則

Box Drive上の次のファイルを復元可能な正本とします。

- PDF
- `metadata.json`
- `note.md`
- キーワード設定
- 引用関係
- アプリ設定

SQLiteはローカルの検索・表示用インデックスです。

- SQLiteをBox Drive内へ保存しないでください。
- SQLiteが失われても、Box Drive上の正本から再構築可能にしてください。
- テストで実際のBox Driveへ書き込まないでください。
- テストには一時ディレクトリを使用してください。
- 保存先パスやWindowsユーザー名をハードコードしないでください。
- Box Driveが利用できない場合、別の場所へ勝手に保存しないでください。

## ファイル操作の安全性

- JSON、Markdown、PDFの保存は可能な限りアトミックに行ってください。
- 一時ファイルへ保存し、検証後に正式ファイルへ置換してください。
- パストラバーサルを防止してください。
- 外部入力されたパスをそのまま実行しないでください。
- 登録済みIDから管理対象ファイルを安全に解決してください。
- 不完全な論文データを通常一覧へ表示しないでください。
- 削除は原則として`trash`への移動とし、即時完全削除を避けてください。
- 破壊的なコマンドを実行しないでください。

## 実装方針

- 変更前に関連コードとテストを確認してください。
- 最小限の変更で目的を達成してください。
- 無関係なリファクタリングを混ぜないでください。
- ユーザーの既存変更を保持してください。
- 固定モック、空実装、見せかけの成功表示を完成扱いにしないでください。
- エラーを握りつぶさず、原因と対処方法をユーザーへ表示してください。
- 外部サービスへ論文情報、メモ、検索語を送信しないでください。
- テレメトリーを追加しないでください。

## UI方針

- 起動時はライブラリ画面を表示します。
- 主要操作は画面上で見つけやすくしてください。
- 入力項目の多い操作を小さなモーダルへ押し込めないでください。
- 一覧は簡潔な表形式を基本とします。
- 詳細な情報は論文詳細画面へ分離してください。
- 長時間処理は、読みやすい1つの全体進捗として表示してください。
- Windowsの表示倍率125～200%で操作できるようにしてください。
- 初版ではダークモードを必須としません。

## 検索とグラフ

フリーテキスト検索の対象は次の3項目だけです。

- タイトル
- 著者
- キーワード

要旨、メモ、PDF本文、DOI、URL、備考を検索対象へ追加しないでください。

ナレッジグラフは次の2種類です。

- キーワードグラフ
- 引用グラフ

グラフに固定のノード上限を設けたり、条件に一致するノードを黙って省略したりしないでください。

## テスト

変更内容に応じて、少なくとも以下を確認してください。

- バックエンドテスト
- フロントエンドテスト
- TypeScript型チェック
- フロントエンドビルド
- 必要に応じたブラウザ動作確認

ファイル保存に関するテストでは、以下を確認してください。

- 正常保存
- 重複判定
- 途中失敗時の後始末
- SQLite再構築
- ごみ箱と復元
- 不正パスの拒否

実行していない確認を、実行済みとして報告しないでください。

## バージョンとドキュメント

- バージョン番号は1か所を正本として管理してください。
- 動作や設定方法を変更した場合は`README.md`を更新してください。
- 技術判断を変更した場合は`docs/technical-requirements.md`を更新してください。
- リリース内容は`CHANGELOG.md`へ記録してください。
- 未実装項目や既知の制約を明記してください。

## Git運用

- コミットメッセージは日本語で、変更内容が分かる表現にしてください。
- コミット、タグ、pushはそれぞれ別の操作として扱ってください。
- ユーザーから依頼されていないコミット、タグ、pushを行わないでください。
- コミット前に変更範囲とテスト結果を確認してください。
- 無関係なファイルをコミットに含めないでください。
- `git reset --hard`や既存変更を破棄する操作を行わないでください。

## 完了報告

作業完了時は次を簡潔に報告してください。

- 実装・変更した内容
- 主な変更ファイル
- 実行したテストと結果
- 実行していない確認
- 既知の制約
- 次に行うべき作業
